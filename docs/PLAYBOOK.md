# پیاده‌سازی مرحله‌به‌مرحله — دستورهای تست برای کاربر

این فایل فقط برای «چطور کدها را به‌روز کنم و کجا ببینم» است. هیچ دستوری اینجا لازم نیست حفظ شود؛
در پایان هر فاز، دقیقاً همان دستورهای لازم در چت نوشته می‌شود.

## ۱) به‌روزرسانی کدها (هر بار که فاز جدیدی تمام شد)

در پوشه‌ی پروژه (روی ویندوز: کلید `Shift` را نگه‌دار، راست‌کلیک روی فضای خالی پوشه →
«Open PowerShell window here» / «باز کردن پنجره‌ی اینجا»):

```powershell
git pull
npm install
```

اگر `npm install` چیزی نصب نکند، فقط یک خط ساده نشان می‌دهد و تمام — این طبیعی است.

## ۲) بالا آوردن پروژه

```powershell
npm run dev
```

وقتی این خطوط را دیدی یعنی آماده است:

```
▲ Next.js 15.5.25
- Local: http://localhost:3000
✓ Ready in ...
```

برای خاموش کردن: در همان پنجره کلیدهای `Ctrl` + `C`.

## ۳) کجا نگاه کنم؟

| آدرس | چه چیزی را نشان می‌دهد |
|---|---|
| `http://localhost:3000/fa` | صفحه‌ی اول، فارسی (چپ‌به‌راست نیست؛ راست‌به‌چپ است) |
| `http://localhost:3000/en` | همان صفحه، انگلیسی |
| `http://localhost:3000/fa/ui-gallery` | **گالری همه‌ی اجزا** — مهم‌ترین صفحه در فاز ۱ |
| `http://localhost:3000/en/ui-gallery` | همان گالری، انگلیسی |
| `http://localhost:3000/fa/swap` | صفحه‌ی «مبادله» (فعلاً جای خالی/placeholder است) |
| `http://localhost:3000/fa/governance` | صفحه‌ی «حاکمیت» (placeholder) |

بقیه‌ی بخش‌ها هم به همین شکل‌اند: `/fa/pools` ، `/fa/stake` ، `/fa/farm` ، `/fa/bridge` ، `/fa/portfolio`.

> روی مک یا لینوکس به‌جای `PowerShell` همان «Terminal» را باز کن؛ دستورهای داخل کادر
> ``` بدون تغییر هستند.

## ۴) قاعده‌ی رفرش

بعد از هر بار `git pull`، در مرورگر یک بار `Ctrl` + `Shift` + `R` بزن (ریفرش کامل).
اگر صفحه سفید یا خطا داد، `npm run dev` را با `Ctrl` + `C` ببند و دوباره باز کن.
اگر باز هم درست نشد، این دو خط را بزن و دوباره `npm run dev`:

```powershell
Remove-Item -Recurse -Force .next
npm run dev
```

## ۵) هر بار که فازی تمام شد، چه چیزی در چت می‌نویسم؟

سه چیز، همیشه:
1. چه چیزی ساخته/اصلاح شده (فارسی ساده).
2. دستورهای دقیق: `git pull` → `npm install` → `npm run dev`.
3. آدرس یا آدرس‌هایی که باید باز کنی و آنچه باید در هر کدام ببینی (چک‌لیست چشمی).
